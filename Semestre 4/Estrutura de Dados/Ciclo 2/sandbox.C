#include <stdio.h>
#include <stdlib.h>

// criando o struct com o dado presente no Node e um ponteiro 
typedef struct Node {
    int data;
    struct Node * next;
};

int main (){
    printf("bla");
}